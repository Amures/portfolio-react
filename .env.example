# Access key for the contact form (https://web3forms.com — free, no account needed).
# Without it the form shows a fallback message pointing at the email address
# instead of failing silently.
VITE_WEB3FORMS_ACCESS_KEY=
